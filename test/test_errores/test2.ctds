class main {
	int inc(int x) {
		return x + 1;
	}
	int read_int() extern;
	void print(boolean s) extern;

	void main() {
		int y;
		y = read_int();
		y = inc(y);
		if (y == 1)
			print(y==1);
		else
			print(y!=1);
